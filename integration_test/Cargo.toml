[package]
name = "integration-test"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["qemu"]
std = ["reservoir-core/std", "reservoir-infer/std", "nalgebra/std"]
qemu = []

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "test_wasm"
path = "src/bin/test_wasm.rs"
test = false
bench = false

[[bin]]
name = "test_arm"
path = "src/bin/test_arm.rs"
required-features = ["qemu"]
test = false
bench = false

[[bin]]
name = "test_x86"
path = "src/bin/test_x86.rs"
required-features = ["qemu"]
test = false
bench = false

[[bin]]
name = "test_riscv"
path = "src/bin/test_riscv.rs"
required-features = ["qemu"]
test = false
bench = false

[dependencies]
reservoir-core = {  path = "../crates/core", default-features = false, features = [
    "libm",
] }
reservoir-infer = {  path = "../crates/infer", default-features = false, features = [
    "libm",
] }
nalgebra = { version = "0.33.2", default-features = false, features = ["libm"] }
heapless = "0.9.2"

[build-dependencies]
reservoir-core = {  path = "../crates/core", features = ["std"] }
reservoir-train = {  path = "../crates/train", features = ["std"] }
reservoir-datasets = { path = "../crates/datasets", features = ["std"] }
nalgebra = { version = "0.33.2", features = ["std"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"

[target.'cfg(target_arch = "wasm32")'.dependencies.web-sys]
version = "0.3"
features = ["console"]

[target.'cfg(target_arch = "arm")'.dependencies]
cortex-m = "0.7"
cortex-m-rt = "0.7"
cortex-m-semihosting = "0.5"
panic-semihosting = "0.6"

[target.'cfg(target_arch = "x86")'.dependencies]
panic-halt = "0.2"
x86 = "0.52"

[target.'cfg(target_arch = "riscv32")'.dependencies]
ufmt = "0.2"
riscv = "0.11"
riscv-rt = "0.12"
panic-halt = "0.2"

[profile.dev]
panic = "abort"
lto = true
opt-level = "s"

[profile.release]
panic = "abort"
codegen-units = 1
debug = true
lto = true
opt-level = "s"
